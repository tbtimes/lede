import { ProjectReport, Dependency } from '../interfaces'


export class Lede {
    constructor(public compilers: any, public deployer: any) {}

    buildProject(projectRoot: string): any /*ProjectReport*/ {
        
    }
}